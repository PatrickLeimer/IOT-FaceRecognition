# Backend URL — set to your laptop's LAN IP before running with the XIAO
# Find it with: ipconfig (Windows) or ipconfig getifaddr en0 (macOS)
# Example: VITE_BACKEND_URL=http://192.168.1.42:8000
VITE_BACKEND_URL=http://172.20.10.2:8000